{{ define "content" }}
	<div class="wcag container">
		<h1>{{ .Title }}</h1>
		{{ .Content | markdown }}
		
		<!-- TOC -->
		<nav role="navigation" class="table-of-contents" id="toc">
			<h5>Indice</h5>
			<ol>
				{{ range .SubSections }}
					<li>
						<a href="#{{ .Source }}">{{ .Title }}</a>
						<ol>
							{{ range .SubSections }}
								<li>
									<a href="#{{ .Source }}">{{ .Title }}</a>
									<ol>
										{{ range .SubSections }}
											<li><a href="#{{ .Source }}">{{ .Title }}</a></li>
										{{ end }}
									</ol>
								</li>
							{{ end }}
						</ol>
					</li>
				{{ end }}
			</ol>
		</nav>
	
		{{ range .SubSections }}
			<section class="principle">
				<h2>{{ .Title }} <a href="#{{ .Source }}" title="Link permanente a {{ .Title }}">§</a></h2>
				{{ .Content | markdown }}
				
				{{ range .SubSections }}
					<section class="guideline">
						<h3>{{ .Title }} <a href="#{{ .Source }}" title="Link permanente a {{ .Title }}">§</a></h3>
						{{ .Content | markdown }}
						
						{{ range .SubSections }}
							<section class="success-criterion {{ .OutcomeClassName }}">
								<header>
									<h4 id="{{.Source}}">{{ .Title }} <a href="#{{ .Source }}" title="Link permanente a {{ .Title }}">§</a></h4>
									<p>Livello: {{.Level}}</p>
									<p>
										<a target="_blank" title="apri link esterno" href="https://www.w3.org/Translations/WCAG21-it/#{{.Source}}">
											spec
											<i aria-hidden="true" class="fas fa-external-link-alt"></i>
											<span class="sr-only">(apri link esterno)</span>
										</a>
									</p>
								</header>
								<strong><p class="outcome">Esito:</strong> {{ .OutcomeNL }}</p>
								{{ .Content | markdown }}
								
								<p class="goto-toc"><a href="#toc">&uarr; Ritorna all'indice</a></p>
							</section>
						{{ end }}
					</section>
				{{ end }}
			</section>
		{{ end }}
	</div>
{{ end }}